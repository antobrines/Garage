<%= form_with(model: voyage, local: true) do |form| %>
  <% if voyage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voyage.errors.count, "error") %> prohibited this voyage from being saved:</h2>

      <ul>
        <% voyage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :depart %>
    <%= form.text_field :depart %>
  </div>

  <div class="field">
    <%= form.label :arrivee %>
    <%= form.text_field :arrivee %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
